import { View, Text } from 'react-native';
import React from 'react';

import { base } from '../Styles/index';
import rootNavigation from '../types/rootNavigation';

export default function Favorites({ navigation } : rootNavigation) {
    return (
        <View>
            <Text>Rakkc</Text>
            <Text>Rakkc</Text>
            <Text>Rakkc</Text>
        </View>
    );
}
