import Delay from '../../interfaces/Idelay';

type delayBoxFavorites = {
    fromDate: string,
    lateFromDate: string,
    delay: Delay,
    from: string,
    to: string,
};

export default delayBoxFavorites;
