import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    base: {
    },

    input: {
        fontSize: 20,
        marginBottom: 28,
        borderWidth: 1,
        padding: 10,
        borderColor: '#ccc',
        borderRadius: 3,
    }
});

export {styles};
